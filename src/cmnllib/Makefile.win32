INCDIR = ../

CC = gcc -DPLATFORM_WIN32 -DNO_TYPEDEF_BOOL -I $(INCDIR) -Wall
AR = ar -ru
RM = rm -f

OBJS    = cmnllib.o

cmnllib.a : $(OBJS) 
	$(AR) cmnllib.a $(OBJS)
	
cmnllib.o: cmnllib.c
	$(CC) -c cmnllib.c -o cmnllib.o

clean :
	$(RM) *.o *.a
	
